// document.querySelectorAll('.text__input').forEach((element) => {
//   element.addEventListener('blur', (event) => {
//     if (event.target.value !== '') {
//       const filled = event.target.nextElementSibling.classList.add('filled')

//     } else {
//       event.target.nextElementSibling.classList.remove('filled')
//     }
//   });
// });